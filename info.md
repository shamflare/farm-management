# معلومات التشغيل والدخول

> ملف عملي سريع: روابط الدخول، الحسابات، والملاحظات المهمة أثناء التجربة.
> آخر تحديث: 2026-08-22

## 🔗 الروابط (السيرفر شغّال الآن)

| الواجهة | الرابط |
| --- | --- |
| **لوحة الإدارة (ادخل من هنا)** | <http://localhost:3000> |
| واجهة الـ API | <http://127.0.0.1:8000/api/v1/> |
| توثيق الـ API التفاعلي | <http://127.0.0.1:8000/api/docs/> |
| فحص الصحة | <http://127.0.0.1:8000/api/v1/health/> |
| من جهاز آخر على نفس الشبكة | <http://10.235.206.57:3000> |

## 👤 حسابات الدخول

| المستخدم | كلمة المرور | الدور |
| --- | --- | --- |
| `owner` | `farm1234` | مالك — كل الصلاحيات |
| `worker` | `farm1234` | عامل |
| `accountant` | `farm1234` | محاسب |
| `partner` | `farm1234` | شريك |

المزرعة النشطة: **مزرعة الأمل** — فرعان (تربية وتسمين)، مستودعا أعلاف،
٣٠ يوم حليب، وتكاليف تأسيسية.

> الحسابات الإضافية تُنشأ من: **الإعدادات ← المستخدمون والدخول**.
> `python manage.py seed_demo --reset` يمسح مزرعة العرض ويعيد بناءها من الصفر،
> فأي مستخدم أنشأته من الواجهة يفقد عضويته فيها.

## ▶️ كيف أشغّل السيرفر مرة ثانية

```bash
# 1) الخلفية — من مجلد backend
cd D:\farm\backend
..\.venv\Scripts\python manage.py runserver 8000

# 2) لوحة الإدارة — من مجلد admin-web
cd D:\farm\admin-web
npm run dev
```

يجب أن يعمل الاثنان معًا: اللوحة على 3000 تنادي الـ API على 8000.

## ⚠️ ملاحظات مهمة

- **القاعدة الحالية SQLite** في `backend/db.sqlite3` (لأنه لا يوجد ملف `.env`).
  للانتقال إلى PostgreSQL: `docker compose up -d db` ثم `cp .env.example .env`.
- **لا تشغّل `seed_demo --reset`** إلا إذا أردت مسح كل البيانات الحالية والبدء من الصفر —
  سيحذف المستخدمين المضافين يدويًا (مثل `firas`) وكل الحركات المالية.
- **`DEBUG=1` و `ALLOWED_HOSTS=*`** — وضع تطوير فقط، غير صالح للنشر الحقيقي.
- الدخول عبر **JWT**: `POST /api/v1/auth/login/` بـ `{"username","password"}`
  يعيد `access` و `refresh`، والتجديد عبر `/api/v1/auth/refresh/`.
- إذا فتحت اللوحة من جهاز آخر على الشبكة، الـ API لا يزال يشير إلى `127.0.0.1`.
  عالِج ذلك بإنشاء `admin-web/.env.local` يحوي:
  `NEXT_PUBLIC_API_URL=http://10.235.206.57:8000/api/v1`
  (وعنوان الشبكة يتغير عند تغيّر الواي فاي).
- **لا يوجد حذف للتاريخ المالي**: القيد المرحّل يُعكس بقيد مضاد ولا يُحذف.
- تشغيل الاختبارات: `cd backend && ..\.venv\Scripts\python manage.py test tests`

## 🌍 النشر على Render (لمشاركة الموقع مع الأصدقاء)

الكود جاهز للنشر — بقيت خطوات تحتاج حساباتك أنت.

**الحالة:** ✅ الملفات جاهزة ومُختبَرة على PostgreSQL · ⏳ بانتظار ربط GitHub و Render

### الخطوة ١ — ارفع الكود على GitHub

في **الطرفية عندك** (تحتاج تسجيل دخول تفاعلي، لا أستطيع تنفيذه بالنيابة عنك):

```bash
gh auth login          # اختر: GitHub.com → HTTPS → Login with a web browser
```

ثم أخبرني وأتولى إنشاء المستودع والرفع، أو نفّذها بنفسك:

```bash
cd D:arm
gh repo create farm-management --public --source=. --remote=origin --push
```

### الخطوة ٢ — أنشئ الخدمات على Render

1. سجّل في <https://render.com> (مجاني، بحساب GitHub).
2. **New → Blueprint** واختر المستودع. سيقرأ Render ملف `render.yaml` ويقترح
   ثلاث خدمات: `farm-api` و `farm-admin` و `farm-db`.
3. سيطلب منك قيمة **`DEMO_PASSWORD`** — هذه كلمة مرور الحسابات على الإنترنت.
   اختر شيئًا غير `farm1234` وشاركه مع أصدقائك فقط.
4. **Apply** — أول بناء يستغرق ٥–١٠ دقائق.

### الخطوة ٣ — الروابط بعد النشر

| الخدمة | الرابط |
| --- | --- |
| اللوحة (أرسل هذا لأصدقائك) | `https://farm-admin-XXXX.onrender.com` |
| الـ API | `https://farm-api-XXXX.onrender.com/api/v1/` |

> املأ الرابطين هنا بعد أول نشر ناجح — Render يضيف لاحقة عشوائية للاسم.

### ⚠️ حدود الخطة المجانية

- **الخدمة تنام بعد ١٥ دقيقة خمول.** أول زيارة بعد فترة هدوء تستغرق ~٥٠ ثانية
  لكل خدمة. نبّه أصدقاءك ألا يظنوا الموقع معطلًا.
- **قاعدة البيانات المجانية يحذفها Render بعد ٣٠ يومًا** من إنشائها. بعدها
  تنشئ واحدة جديدة، والبذر يعيد ملء بيانات التجربة تلقائيًا.
- الحسابات على الإنترنت هي نفسها `owner` / `worker` / `accountant` / `partner`
  لكن بكلمة مرور `DEMO_PASSWORD` التي اخترتها.
- **بيانات المزرعة الحقيقية لا ترفعها إلى هناك** — هذه نسخة عرض للأصدقاء،
  وأي شخص يملك الرابط وكلمة المرور يدخل كمالك ويعدّل كل شيء.

### ما الذي تغيّر في الكود ليصبح النشر ممكنًا

- `render.yaml` — وصف الخدمات الثلاث.
- `whitenoise` + `gunicorn` — خدمة الملفات الثابتة وتشغيل الإنتاج.
- `bootstrap_demo` — يبذر مرة واحدة فقط (على قاعدة لا مزرعة فيها) ويضبط
  كلمة المرور من `DEMO_PASSWORD` عند كل إقلاع.
- الإعدادات تتعرف على الوسيط أمامها: اسم المضيف و HTTPS والكوكيز الآمنة.

## 🧭 من أين أبدأ التجربة

1. ادخل بـ `owner` / `farm1234`.
2. **لوحة المعلومات** — الأرصدة والأرقام محسوبة من القيود مباشرة.
3. **العمليات** — سجّل مصروفًا أو إيرادًا وراقب أثره الفوري على الصناديق.
4. **الحيوانات** — افتح ملف حيوان: النسب، الأوزان، الصحة، السجل الزمني.
5. **التقارير** — ميزان المراجعة، الأرباح، التدفق النقدي، تقرير القطيع.
6. **الإعدادات** — القوائم، الحقول المخصصة، الأدوار، المستخدمون، الهوية البصرية.

## 🗺️ الشاشات

| الشاشة | ما تفعله |
| --- | --- |
| الرئيسية | بطاقات قابلة للتخصيص + تنبيهات + ربح كل فرع |
| الحيوانات | القائمة، وصفحة كل حيوان: لقاح، ولادة، وزن، نقل بين الفروع، نفوق، صور |
| شراء الحيوانات | عملية شراء بعدة رؤوس، النقل والعمولة محمَّلة على القيمة |
| بيع الحيوانات | بيع بعدة رؤوس، وسبب البيع يحدد بند الإيراد |
| الحليب | الكمية اليومية + مبيعات الحليب ومشتقاته |
| مستودعات الأعلاف | استلام، صرف، تحويل، جرد، هدر، وإضافة أصناف ومستودعات |
| المالية | مصروف، إيراد، تحويل، اعتماد، عكس، حذف نهائي |
| التكاليف التأسيسية | سجل ما بُنيت به المزرعة، خارج أرباح الشهر |
| الأشخاص والحسابات | شركاء وعاملون وموردون وزبائن، وتسديد وتحصيل |
| التقارير | مقارنة الفروع، ميزان المراجعة، الأرباح، التدفق النقدي، القطيع |
| الرصيد الافتتاحي | ما تملكه المزرعة وما عليها يوم بدأت |
| الاعتماد والنسخ | حدود الاعتماد + تنزيل نسخة احتياطية |
| القوائم والبنود · بناء النماذج · المستخدمون · الهوية البصرية | الإعدادات |
| سجل التدقيق | من فعل ماذا ومتى |
