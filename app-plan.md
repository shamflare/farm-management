# خطة تطبيق الجوال — زاد

> تطبيق أندرويد (APK) لمزرعة زاد، يعمل على نفس الـ API الذي تعمل عليه اللوحة.
> بلا Google Play: ملف واحد يُنزَّل ويُثبَّت.
> آخر تحديث: 2026-08-24

---

## ١. لماذا تطبيق أصلًا

اللوحة على `zadfarm.net` تعمل من الجوال، لكنها لوحة إدارة: جداول عريضة، وتقارير
تُقرأ جالسًا. التطبيق شيء آخر — **يُستعمل واقفًا في الحظيرة، بيد واحدة**:

| اللوحة (ويب) | التطبيق (جوال) |
| --- | --- |
| الإعدادات، بناء النماذج، الصلاحيات، الهوية | التسجيل السريع في الميدان |
| التقارير الطويلة والتحليل | نظرة يومية سريعة |
| المحاسبة التفصيلية والمراجعة | مصروف، وزن، ولادة، علف، حليب |
| شاشة كبيرة وفأرة | إبهام واحد، شمس، ويد متّسخة |

**المبدأ الحاكم**: التطبيق لا يُعيد بناء اللوحة في شاشة صغيرة. يأخذ منها ما
يُسجَّل في الميدان، ويعرضه بشكل يُقرأ من نصف متر.

---

## ٢. القرارات التقنية

| القرار | الاختيار | السبب |
| --- | --- | --- |
| الإطار | **React Native عبر Expo** | تحديث المكتبات وبناء الـ APK بأمر واحد، وكاميرا وتخزين آمن جاهزة |
| التوجيه | **expo-router** | كل شاشة ملف، مثل Next.js في اللوحة تمامًا |
| اللغة | TypeScript | نفس عقود البيانات المكتوبة في اللوحة |
| الحالة | React Query (TanStack) | نفس فكرة «اعرض المحفوظ ثم حدّث» التي عالجت بطء اللوحة |
| التخزين | `expo-secure-store` للتوكن · `AsyncStorage` للمحفوظ | كلمة الدخول لا تُخزَّن، التوكن وحده وفي مخزن مشفّر |
| الخطوط | `@expo-google-fonts/*` | نفس خطوط اللوحة: القاهرة وتجوال والمراعي… |
| الحركة | `react-native-reanimated` | انتقالات ناعمة بلا تأخير في الإطارات |
| الرسوم | `react-native-svg` + `victory-native` | مخططات الحليب والأرباح |
| الاتجاه | RTL مفروض (`I18nManager.forceRTL`) | التطبيق عربي بالكامل |

### لماذا Expo لا React Native العارية

بناء APK من مشروع عارٍ يحتاج ضبط Gradle وتوقيعًا يدويًا وصيانة كل ترقية.
Expo يعطي `expo prebuild` + `gradlew assembleRelease` مع كل ذلك مضبوطًا،
ويبقى الخروج منه ممكنًا في أي لحظة (المشروع يبقى React Native حقيقيًا).

---

## ٣. لا شيء جديد في الخادم

التطبيق **لا يحتاج API جديدًا**. كل ما يلزمه موجود ومُختبَر:

```
POST /auth/login/            الدخول (JWT)
GET  /auth/me/               من أنا، وأي مزرعة، وما صلاحياتي
GET  /theme/                 الألوان والخط والاستدارة — نفس عقد اللوحة
GET  /reports/dashboard/     أرقام اليوم
GET  /animals/               القطيع (فلترة بالفرع والحالة والجنس)
POST /animals/               حيوان جديد
GET  /animals/{id}/timeline/ سجل الحيوان
POST /ops/expense/           مصروف
POST /ops/income/            إيراد
POST /ops/milk-production/   حليب اليوم
POST /ops/stock-issue/       صرف علف
POST /births/ · /weights/ · /health/
POST /attachments/upload/    صورة من الكاميرا (data URI)
GET  /alerts/                ما يحتاج انتباهًا
```

الترويسات نفسها: `Authorization: Bearer …` و `X-Farm: zadfarm`.

> **الهوية البصرية تأتي من الخادم.** غيّر لون المزرعة أو خطها من اللوحة، فيتغيّر
> التطبيق عند أول تشغيل — بلا APK جديد. هذا مكتوب في `docs/architecture.md`
> منذ اليوم الأول، والتطبيق يحقّقه أخيرًا.

---

## ٤. التصميم — «خيالي جميل جدًا»

### القاعدة الأولى: لا جداول. أبدًا.

المطلوب صراحةً: **لا سكرول أفقي، ولا صف يُقرأ نصفه**. فكل «صف» يصير **بطاقة
مستقلة** تُظهر كل ما يهم دفعة واحدة:

```
┌────────────────────────────────────────┐
│  ● TR-0014            🟢 موجودة        │   ← الرقم + الحالة (لون + كلمة)
│  نعجة العوّاسي                          │   ← الاسم
│  ─────────────────────────────────────  │
│  🏠 التربية    ♀ أنثى    ⚖ 46 كغ       │   ← ثلاث حقائق في سطر واحد
│  🎂 سنتان                               │
└────────────────────────────────────────┘
```

قواعد البطاقة:
1. **سطر العنوان**: المعرّف (رقم الحيوان/المبلغ/التاريخ) + شارة الحالة.
2. **سطر الهوية**: الاسم أو الوصف، بخط أثقل.
3. **شريط الحقائق**: ٣–٤ حقائق بأيقونات، تلتفّ ولا تُقصّ.
4. **ما لا يُقرأ من نصف متر لا يوضع في البطاقة** — يُترك لصفحة التفصيل.
5. الضغط المطوّل يفتح إجراءات سريعة (وزن، صورة، نقل).

### هوية بصرية للتطبيق

- **الخلفية**: متدرّج هادئ جدًا من لون الهوية (أخضر) إلى الأبيض/الأسود.
- **البطاقات**: زوايا ٢٠ بكسل، ظل ناعم منخفض، حدود شبه شفافة.
- **الرأس**: متدرّج بلون المزرعة، مع اسمها وشعارها، ويصغر عند التمرير.
- **الشريط السفلي**: خمس أيقونات + زر مركزي بارز للتسجيل السريع (＋).
- **الوضع الداكن**: يتبع الجهاز، والألوان مشتقّة من نفس رموز الخادم.
- **الحركة**: ظهور البطاقات بتتابع ٤٠ ملّي ثانية، ضغطة تُصغّر البطاقة قليلًا،
  اهتزاز خفيف (haptics) عند الحفظ.
- **الانتظار**: هياكل عظمية (skeletons) بشكل البطاقة نفسها، لا دوّارة.
- **الأرقام**: خط عريض وكبير — الأرقام هي المنتَج في هذا التطبيق.

### شبكة الشاشة

| العنصر | القياس |
| --- | --- |
| هامش الشاشة | ١٦ |
| فراغ بين البطاقات | ١٢ |
| حشو البطاقة | ١٦ |
| أصغر مساحة لمس | ٤٤×٤٤ |
| حجم النص الأساسي | ١٥ · العناوين ١٩ · الأرقام الكبيرة ٣٢ |

---

## ٥. الشاشات

### المرحلة الأولى — «أراه وأسجّله» (الحد الأدنى القابل للاستعمال)

| # | الشاشة | ماذا تفعل |
| --- | --- | --- |
| ١ | **الدخول** | اسم المستخدم وكلمة المرور، وحفظ الجلسة، وبصمة اختيارية لاحقًا |
| ٢ | **الرئيسية** | بطاقات: النقد، ربح الشهر، عدد القطيع، حليب اليوم، تنبيهات |
| ٣ | **القطيع** | بطاقات الحيوانات + تبويبات الفروع + بحث + فلاتر في ورقة سفلية |
| ٤ | **ملف الحيوان** | صورته، حقائقه، سجله الزمني، وأزرار: وزن · لقاح · ولادة · نفوق |
| ٥ | **تسجيل سريع** | زر ＋ يفتح: مصروف · إيراد · حليب · علف · وزن |
| ٦ | **حسابي** | الاسم، الدور، المزرعة، الخروج، والوضع الداكن |

### المرحلة الثانية — «أدير»

| # | الشاشة | ماذا تفعل |
| --- | --- | --- |
| ٧ | **المال** | بطاقات الحركات، تصفية بالنوع، وتفصيل القيد |
| ٨ | **الحليب** | إدخال اليوم + مخطط آخر ٣٠ يومًا |
| ٩ | **الأعلاف** | أرصدة المستودعات، صرف واستلام |
| ١٠ | **الأشخاص** | من له ومن عليه، وتسديد سريع |
| ١١ | **التقارير** | مقارنة الفروع والأرباح، مبسّطة بالمخططات |

### المرحلة الثالثة — «الميدان بلا شبكة»

| # | الميزة | ماذا تفعل |
| --- | --- | --- |
| ١٢ | **الكاميرا** | صورة الحيوان أو الفاتورة من داخل النموذج |
| ١٣ | **صندوق الصادر** | تسجيل بلا إنترنت، ويُرسل عند عودتها (مفاتيح idempotency جاهزة في الخادم) |
| ١٤ | **الباركود/الشريحة** | قراءة رقم الحيوان بالكاميرا |
| ١٥ | **إشعارات** | تنبيهات اللقاح والولادة المتوقّعة |

---

## ٦. البنية داخل المشروع

```
mobile/
  app/                      # expo-router: كل ملف شاشة
    (auth)/login.tsx
    (tabs)/index.tsx        # الرئيسية
    (tabs)/animals.tsx
    (tabs)/money.tsx
    (tabs)/more.tsx
    animal/[id].tsx
    _layout.tsx
  src/
    api/client.ts           # نفس عقد lib/api.ts في اللوحة
    api/queries.ts          # React Query: مفاتيح واستعلامات
    theme/tokens.ts         # يحوّل رموز الخادم إلى سمة React Native
    theme/ThemeProvider.tsx
    ui/Card.tsx             # البطاقة الأساسية
    ui/DataCard.tsx         # «صف الجدول» كبطاقة
    ui/Stat.tsx  Badge.tsx  Sheet.tsx  Skeleton.tsx  Button.tsx
    forms/                  # نماذج التسجيل السريع
    lib/format.ts           # أرقام وتواريخ عربية بأرقام لاتينية
```

**قاعدة**: كل ما في `src/ui` لا يعرف شيئًا عن المزرعة — يعرف الشكل فقط.
وكل ما في `app/` لا يكتب لونًا ولا مقاسًا بيده، بل يأخذه من السمة.

---

## ٧. كيف يصير الـ APK

### الطريق (أ) — بناء محلي على جهازك

مرة واحدة:
```powershell
winget install EclipseAdoptium.Temurin.17.JDK       # جافا ١٧
winget install Google.AndroidStudio                  # أو أدوات السطر فقط
```
ثم في كل إصدار:
```powershell
cd D:\farm\mobile
npx expo prebuild --platform android --clean
cd android
./gradlew assembleRelease
# الناتج: android/app/build/outputs/apk/release/app-release.apk
```

- ✅ بلا حسابات، بلا انتظار طوابير، وبلا حدود عدد.
- ⚠️ يحتاج ~٤ غيغا تنزيلًا أول مرة.

### الطريق (ب) — بناء سحابي EAS [مُختار]

الإعداد جاهز في `mobile/eas.json`: ملف **APK** لا AAB (الأخير لا يُثبَّت إلا
عبر المتجر)، وتوزيع داخلي.

```bash
cd D:\farm\mobile
npx eas-cli login                                  # مرة واحدة — تسجيل تفاعلي
npx eas-cli build -p android --profile preview     # يعيد رابط تنزيل APK
```

- ✅ بلا تثبيت أي شيء على الجهاز.
- ⚠️ يحتاج حساب Expo مجاني، والبناء ينتظر دورًا في الطابور (عادة دقائق).
- مفتاح التوقيع يُنشئه EAS ويحفظه في حسابك — لا تفقده، فبه وحده تُحدَّث
  النسخ فوق بعضها.

### التوقيع والتوزيع

- مفتاح توقيع واحد يُنشأ مرة ويُحفظ (**بلا نسخة منه لا يمكن تحديث التطبيق
  فوق نفسه** — يُحفظ مع النسخ الاحتياطية).
- التوزيع: ارفع الـ APK إلى `zadfarm.net/app` أو أرسله عبر واتساب.
- المستخدم يفعّل «تثبيت من مصدر غير معروف» مرة واحدة.
- **تحديث التطبيق**: `expo-updates` يسمح بدفع تحديثات JS بلا APK جديد —
  يُدرس في المرحلة الثانية.

---

## ٨. المخاطر وما يقابلها

| الخطر | العلاج |
| --- | --- |
| الشبكة تنقطع في الحظيرة | صندوق صادر محلي + مفاتيح idempotency (الخادم جاهز لها) |
| شاشة صغيرة وبيانات كثيرة | البطاقة تعرض ٤ حقائق، والباقي في التفصيل |
| RTL يكسر التخطيطات | `start/end` بدل `left/right` في كل مكان، وفحص مبكر |
| بطء التطبيق مع كثرة الحيوانات | `FlashList` وقوائم مُحوَّرة، وصفحات ٥٠ |
| الشمس تُعمي الشاشة | تباين عالٍ في الوضع الفاتح، وخط عريض للأرقام |
| APK يكبر | تقسيم حسب المعمارية (`arm64-v8a` وحده يكفي للأجهزة الحديثة) |

---

## ٩. مراحل التنفيذ

- [x] **م٠ · الأساس** — مشروع Expo في `mobile/`، RTL مفروض، خط القاهرة،
      السمة تُقرأ من الخادم، عميل API بتوكن في مخزن مشفّر وتجديد تلقائي،
      وشاشة دخول تعمل على `zadfarm.net` فعلًا.
- [x] **م١ · القراءة** — الرئيسية ببطاقات الأرقام والفروع والتنبيهات، القطيع
      ببطاقات وتبويبات فروع وبحث، وملف الحيوان بسجله الزمني.
- [x] **م٢ · الكتابة (الأساسية)** — تسجيل سريع: مصروف · إيراد · حليب اليوم.
      يبقى منها: الوزن والولادة واللقاح وصرف العلف.
- [x] **م٣ · أول APK** — بُني سحابيًا عبر EAS ونُشر على
      <https://zadfarm.net/app> بزر في رأس اللوحة. الإصدار 1.0.0 · 106 ميغا.
      **يبقى**: تصغير الحجم (بناء لمعماريتين بدل أربع + تصغير الشيفرة).
- [ ] **م٤ · التوسّع** — المال، الأعلاف، الأشخاص، التقارير بالمخططات.
- [ ] **م٥ · الميدان** — الكاميرا، العمل بلا شبكة، الإشعارات.

### ما بُني فعلًا (م٠–م٢)

```
mobile/
  app/_layout.tsx          الجذر: خطوط، سمة، حارس الجلسة، RTL
  app/(auth)/login.tsx     الدخول
  app/(tabs)/index.tsx     الرئيسية
  app/(tabs)/animals.tsx   القطيع — بطاقات وتبويبات
  app/(tabs)/record.tsx    التسجيل السريع
  app/(tabs)/more.tsx      الحساب والخروج
  app/animal/[id].tsx      ملف الحيوان
  src/api/                 العميل والاستعلامات
  src/theme/               رموز الخادم → سمة React Native
  src/ui/                  البطاقة، الشارة، الزر، الهيكل، الرأس، الجسم
  scripts/smoke.mjs        فحص عقد الـ API على الخادم الحيّ
```

مُتحقَّق منه: `npm run typecheck` نظيف · الحزمة تُبنى (1721 وحدة) ·
`npm run smoke` يمرّ على ستة مسارات حيّة ويعيد سمة المزرعة.

كل مرحلة تنتهي بشيء **يُفتح ويُستعمل**، لا بشيء «شبه جاهز».

---

## ٩.١ أين وصلنا — سجل الجلسات

> يُقرأ من الأعلى: آخر ما جرى أولًا. من هنا يُستأنف العمل في أي وقت.

### ليلة ٢٤ آب ٢٠٢٦ — ما أُنجز

**التطبيق (مبني ومنشور):**

| الإصدار | ماذا فيه | الحجم |
| --- | --- | --- |
| 1.0.0 | الدخول، الرئيسية، القطيع، ملف الحيوان، تسجيل سريع | ١٠٦ ميغا |
| **1.1.0** ✅ منشور على <https://zadfarm.net/app> | + كل الأقسام، إصلاح الهيدر، أفعال الميدان | **٥٠ ميغا** |

- ✅ إصلاح **الهيدر كان يغطّي أول عنصر** في كل شاشة (سُحب المحتوى تحته، وعلى
  أندرويد الارتفاع يقرر من يُرسم فوق من لا ترتيب الشيفرة).
- ✅ **١٤ شاشة**: المال، الحليب، الأعلاف، الأشخاص، المشتريات، المبيعات،
  التأسيسية، التقارير (٥ تقارير)، التدقيق، القوائم، المستخدمون، الهوية،
  حسابي — مجمّعة في «المزيد» ومخفيّة عمّن لا يملك صلاحيتها.
- ✅ **أفعال الميدان** من ملف الحيوان: وزن · لقاح · ولادة · نفوق.
- ✅ **تصغير الحجم**: معماريتا الهواتف فقط + تصغير الشيفرة والموارد.
- ✅ **زر تنزيل التطبيق** في رأس اللوحة + صفحة `zadfarm.net/app` وسكربت نشر.

**القطيع (في الشيفرة، بانتظار البناء والنشر):**

- ✅ **تعديل كل بيانات الحيوان** من اللوحة، ومنها **رقم الحيوان** — مع رفض
  الرقم المكرّر برسالة عربية.
- ✅ **حذف «إضافة حيوان»**: الحيوان يدخل بشراء أو بولادة فقط، من اللوحة ومن
  التطبيق معًا.
- ✅ **حقول الشراء في قسم الحيوانات**: كيف دخل · تاريخ الدخول · المورد ·
  سعر الشراء · **التكلفة الكاملة** (الثمن + حصّته من النقل والعمولة).
- ✅ بطاقة **«كيف دخل هذا الرأس»** في ملف الحيوان بكل تفاصيل الصفقة.
- ✅ سعر الشراء يُصحَّح لمولود أو لرأس كان موجودًا عند البدء، ويُرفض تعديله
  لرأس دخل بصفقة (المستند هو المرجع، فلا يفترق الدفتر عن الملف).
- ✅ القائمة تجلب الصفقة بـ prefetch: ستون رأسًا = استعلامان لا مئة وعشرون.

### ما تبقّى — يُستأنف من هنا

- [ ] **بناء ونشر إصدار التطبيق 1.2.0** بحقول الشراء الجديدة (الشيفرة جاهزة).
- [ ] **تعديل بيانات الحيوان من داخل التطبيق** (موجود في اللوحة فقط الآن).
- [ ] شاشة **شراء الحيوانات** في التطبيق: تسجيل صفقة كاملة لا عرضها فقط.
- [ ] **صرف العلف** و**تسجيل الوزن الجماعي** من التطبيق.
- [ ] الكاميرا: صورة الحيوان والفاتورة (م٥).
- [ ] العمل بلا شبكة: صندوق صادر ومفاتيح idempotency (م٥).
- [ ] إشعارات اللقاح والولادة المتوقّعة (م٥).

## ١٠. ما لن يدخل التطبيق

قيل صراحةً، حتى لا يتضخّم:

- بناء النماذج والحقول المخصّصة → تبقى في اللوحة.
- إدارة الصلاحيات والأدوار → تبقى في اللوحة.
- الهوية البصرية والقوائم → تبقى في اللوحة.
- الرصيد الافتتاحي وحذف القيود → عمليات خطرة، مكانها شاشة كبيرة.
- سجل التدقيق الكامل → يُقرأ على اللوحة.

التطبيق **نافذة ميدانية**، لا نسخة ثانية من النظام.
